# docker-compose.mcps.yml
# Spin up any subset of MCPs that require a long-running process.
# stdio MCPs (like mcp-blueprint) do NOT go here — they run as child processes.
# Add services here as you build mcp-postgres, mcp-redis, etc.

version: "3.9"

services:
  # ── Data MCPs ────────────────────────────────────────────────────────────
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: ${POSTGRES_USER}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
  #     POSTGRES_DB: ${POSTGRES_DB}
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"

  placeholder:
    image: hello-world
    # Remove this placeholder once real services are added.

# volumes:
#   postgres_data:

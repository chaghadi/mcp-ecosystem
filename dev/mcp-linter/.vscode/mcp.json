{
  "servers": {
    "mcp-linter": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "mcp-linter"
      ],
      "cwd": "${workspaceFolder}/dev/mcp-linter"
    }
  }
}
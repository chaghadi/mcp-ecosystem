# Google Cloud Console → IAM → Service Accounts → create one with
# "Service Account User" role, download JSON key
# Play Console → Setup → API access → Grant access to the service account
GOOGLE_SERVICE_ACCOUNT_PATH=/absolute/path/to/service-account.json
# OR paste the entire JSON contents:
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

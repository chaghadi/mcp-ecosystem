{
  "servers": {
    "mcp-blueprint": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "mcp-blueprint"],
      "cwd": "${workspaceFolder}/dev/mcp-blueprint",
      "env": {
        "BLUEPRINT_DATA_DIR": "${workspaceFolder}/dev/mcp-blueprint/data"
      }
    }
  }
}

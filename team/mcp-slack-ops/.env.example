# Create app at api.slack.com/apps → install to workspace
# OAuth & Permissions → Bot Token Scopes:
#   chat:write, channels:read, users:read, channels:manage, reactions:write
SLACK_BOT_TOKEN=xoxb-your-bot-token

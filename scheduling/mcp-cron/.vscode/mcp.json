{
  "servers": {
    "mcp-cron": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "mcp-cron"
      ],
      "cwd": "${workspaceFolder}/scheduling/mcp-cron"
    }
  }
}